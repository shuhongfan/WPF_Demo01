<Page x:Class="ch04.Examples.FileStreamExample"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="FileStreamPage">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource BorderStyle}">
            <TextBlock Text="FileStream类的基本用法" Style="{StaticResource TitleStyle}"/>
        </Border>
        <Border DockPanel.Dock="Bottom" Style="{StaticResource BorderStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Name="btnRead" Width="60" Content="读文件" Click="btnRead_Click"/>
                <Button Name="btnWrite" Margin="10 0 0 0" Width="60" Content="写文件" Click="btnWrite_Click"/>
            </StackPanel>
        </Border>
        <ScrollViewer>
            <StackPanel Background="White" TextBlock.LineHeight="20">
                <TextBlock x:Name="textBlock1" Margin="0 10 0 0" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Page>
